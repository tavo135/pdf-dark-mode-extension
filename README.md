# PDF Dark Mode (Chrome Extension)

Extensión MV3 que redirige automáticamente URLs que parecen PDF hacia un visor propio con filtros de color (oscuro/sepia/claro).

## Instalación (modo desarrollador)
1. Abre chrome://extensions
2. Activa Developer mode
3. Load unpacked -> selecciona la carpeta
4. (Opcional) En Details: activa "Allow access to file URLs"

## Uso
Abre cualquier URL que termine en .pdf. Serás redirigido al visor.
